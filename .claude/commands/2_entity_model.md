Create an entity model (docs/entity_model.md) based on docs/requirements.md as a Markdown file with an included
Mermaid.js ERD diagram that shows the entity names (no columns) and the relationships between the entities.
Use sequences for the primary keys.